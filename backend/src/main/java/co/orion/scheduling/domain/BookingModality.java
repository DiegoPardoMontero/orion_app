package co.orion.scheduling.domain;

/**
 * Cómo se da la clase. Hoy solo hay una forma.
 *
 * <p>Orión no ofrece clases presenciales, y el CHECK de la base tampoco las admite (V30). Se
 * conserva el enum —en vez de borrar la columna— porque el día que vuelvan es una migración de
 * una línea, y porque el histórico ya escrito sigue diciendo la verdad sobre sí mismo.
 */
public enum BookingModality {
    VIRTUAL
}
