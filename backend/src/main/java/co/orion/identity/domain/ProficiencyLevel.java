package co.orion.identity.domain;

/** Nivel al que un profesor enseña un idioma. Coincide con el CHECK de professor_language_levels. */
public enum ProficiencyLevel {
    BEGINNER,
    INTERMEDIATE,
    ADVANCED
}
