# Java / Maven
target/
!.mvn/wrapper/maven-wrapper.jar
!**/src/main/**/target/
!**/src/test/**/target/
*.class
*.jar
*.war
*.log
hs_err_pid*
replay_pid*

# Node / frontend
node_modules/
dist/
build/
.next/
coverage/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Entorno
.env
.env.*
!.env.example

# IDEs
.idea/
*.iml
*.ipr
*.iws
.vscode/
.settings/
.project
.classpath
nbproject/private/
.DS_Store
Thumbs.db

# Logs descargados de Railway (diagnóstico puntual)
logs.*.json
logs.*.csv

# Metadatos que Windows pega al descargar archivos
*:Zone.Identifier
*.lnk

# Perfiles de navegador que Lighthouse deja tras una auditoría. Se llegaron a commitear 550
# archivos porque la ruta de Windows entra como un único nombre de carpeta con barras invertidas
# dentro, y ningún patrón de directorio la atrapaba.
C:\*
lighthouse.*/

# Resultados de Playwright
test-results/
playwright-report/

# Capturas con credenciales/secretos — nunca al repo
*Credentials*.png
*credenciales*.png
*secret*.png
